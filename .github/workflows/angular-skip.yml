name: Build and Test Frontend

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - 'frontend/**'
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - 'frontend/**'

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No changes in frontend/, skipping."